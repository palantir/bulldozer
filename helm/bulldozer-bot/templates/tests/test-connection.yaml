apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bulldozer-bot.fullname" . }}-test-connection"
  labels:
{{ include "bulldozer-bot.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bulldozer-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
